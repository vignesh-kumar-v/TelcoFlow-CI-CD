[pytest]
testpaths = tests
addopts = -ra --strict-markers
markers =
    integration: needs artifacts from a completed pipeline run; skipped when absent
    gcp: needs real Google Cloud credentials; skipped unless GCP_PROJECT is set
filterwarnings =
    ignore::DeprecationWarning
